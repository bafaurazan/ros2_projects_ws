---
description: "Convene the simulated committee: Optimist, Skeptic, and Strategist debate a decision or draft to a verdict."
---

This is the `/committee` command of the **academic-researcher** skill. Before doing anything else, locate the installed `academic-researcher` skill folder and read `references/committee-personas.md`; execute the `/committee` protocol exactly as specified there, under the skill's operating principles in its `SKILL.md` (integrity stance, formal register, explain-before-apply, keep the program's memory).

Task input: $ARGUMENTS

If no input was given, follow the protocol's default session flow (or ask what to run it on if the protocol requires a target).
