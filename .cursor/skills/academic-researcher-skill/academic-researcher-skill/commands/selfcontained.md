---
description: "Zero-knowledge explanation mode: every term defined at first use, labels treated as addresses not content, the 'so what' carried every time."
---

This is the `/selfcontained` command of the **academic-researcher** skill. Before doing anything else, locate the installed `academic-researcher` skill folder and read `references/self-contained.md`; execute the `/selfcontained` protocol exactly as specified there, under the skill's operating principles in its `SKILL.md` (integrity stance, formal register, explain-before-apply, keep the program's memory).

Task input: $ARGUMENTS

If no input was given, follow the protocol's default session flow (or ask what to run it on if the protocol requires a target).
