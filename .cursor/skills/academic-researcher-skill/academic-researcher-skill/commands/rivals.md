---
description: "Rival-mechanism audit: enumerate every theory predicting the result without your mechanism, steelman each, design the discharging condition, audit the gap claim against primary sources."
---

This is the `/rivals` command of the **academic-researcher** skill. Before doing anything else, locate the installed `academic-researcher` skill folder and read `references/rival-mechanisms.md`; execute the `/rivals` protocol exactly as specified there, under the skill's operating principles in its `SKILL.md` (integrity stance, formal register, explain-before-apply, keep the program's memory).

Task input: $ARGUMENTS

If no input was given, follow the protocol's default session flow (or ask what to run it on if the protocol requires a target).
