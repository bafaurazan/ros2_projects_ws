---
description: "Reading-ledger discipline: one reading log as single source of truth, strike-never-delete clearances, not-held workaround protocol."
---

This is the `/corpus` command of the **academic-researcher** skill. Before doing anything else, locate the installed `academic-researcher` skill folder and read `references/corpus-ledger.md`; execute the `/corpus` protocol exactly as specified there, under the skill's operating principles in its `SKILL.md` (integrity stance, formal register, explain-before-apply, keep the program's memory).

Task input: $ARGUMENTS

If no input was given, follow the protocol's default session flow (or ask what to run it on if the protocol requires a target).
