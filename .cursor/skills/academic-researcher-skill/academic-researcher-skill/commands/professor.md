---
description: "Act as a professor: sequenced learning roadmap, active-recall teaching one sub-milestone per turn, literature synthesis matrix."
---

This is the `/professor` command of the **academic-researcher** skill. Before doing anything else, locate the installed `academic-researcher` skill folder and read `references/professor.md`; execute the `/professor` protocol exactly as specified there, under the skill's operating principles in its `SKILL.md` (integrity stance, formal register, explain-before-apply, keep the program's memory).

Task input: $ARGUMENTS

If no input was given, follow the protocol's default session flow (or ask what to run it on if the protocol requires a target).
