---
description: "Bash: prefiksy is_/has_/get_/set_/try_"
globs: "**/*.{bash,sh}"
alwaysApply: false
---

# Bash naming

Prefiksy jak w [../cpp/naming.mdc](../cpp/naming.mdc) (ta sama logika, snake_case). Bundles `scripts/bash_macros/`: [macros.mdc](macros.mdc).

Prefiks opisuje **typ zachowania**. Używaj go, gdy poprawia czytelność; inaczej zostaw czysty czasownik. Dotyczy **nowego kodu** — nie refaktoruj legacy bez prośby.

## Prefiksy

| Prefiks | Zwraca | Sens | Dobry przykład | Zły przykład |
|---------|--------|------|----------------|--------------|
| `is_` | bool (exit 0/1) | „czy teraz X?” | `is_wsl()`, `is_broken_local_cmake()` | `is_velocity` zwracające liczbę |
| `has_` | bool | „czy istnieje / jest dostępne?” | `has_config()`, `has_command()` | `has_path` gdy zwraca ścieżkę |
| `get_` | wartość | odczyt bez zmiany stanu | `get_project_root()`, `get_host_arch()` | `get_and_source` — ma efekt uboczny |
| `set_` | — | zapis / konfiguracja | `set_workspace_root()`, `set_default_middleware()` | `set_path` gdy tylko odczytuje |
| `try_` | bool | operacja, która może się nie udać | rzadko w bashu | `try_build` gdy `build` zawsze jest komendą |

Prywatne helpery: `_is_foo`, `_has_foo` (np. `_has_src_dir`). Publiczne komendy (`build`, `diag`) — **bez** prefiksu.
