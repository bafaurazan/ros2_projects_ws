# bash_bringup

Workspace CLI router. Public entry remains `./scripts/setup.bash`.

| Path | Role |
|------|------|
| `launch/bringup.bash` | Router: `humble` / `jazzy` / `macros` |
| `src/macros_session.bash` | Host session with macros only (no Distrobox, no ROS) |
